version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    cooldown:
      default-days: 7
    groups:
      all-dependencies:
        patterns:
          - "*"
    open-pull-requests-limit: 10
    ignore:
      # eslint-plugin-react not yet compatible with eslint 10 (issue #45)
      - dependency-name: "eslint"
        versions: [">= 10"]
      - dependency-name: "@eslint/js"
        versions: [">= 10"]
      # vite-plugin-pwa not yet compatible with vite 8 (issue #48)
      - dependency-name: "vite"
        versions: [">= 8"]
      # @vitejs/plugin-react >= 6 requires vite 8 (issue #48)
      - dependency-name: "@vitejs/plugin-react"
        versions: [">= 6"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    cooldown:
      default-days: 7
    groups:
      all-actions:
        patterns:
          - "*"
